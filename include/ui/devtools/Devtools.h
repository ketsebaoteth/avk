#pragma once

#include "core/app/Types.h"
namespace atomic {

/**
 * @brief Renders the high-priority floating DevTools dock button with
 * edge-snapping, smooth spring motion, and smart-direction popup panel.
 */
void drawDevToolsDock(VeraWindow *window);

} // namespace atomic
